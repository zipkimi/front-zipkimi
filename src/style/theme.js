const TEXT = {
  hide: `
    position: absolute;
    clip: rect(0 0 0 0);
    width: 1px;
    height: 1px;
    margin: -1px;
    overflow: hidden;
  `,
};

const COLOR = {
  original: "#BB2649",
  secondOriginal: "#BB264910",
  white: "#FFFFFF",
  secondWhite: "#AEAEAE",
};

const ROUND = {
  xs: "4px",
  sm: "8px",
  md: "16px",
  lg: "50%",
  xl: "100%",
};

const theme = {
  TEXT,
  COLOR,
  ROUND,
};

export default theme;
